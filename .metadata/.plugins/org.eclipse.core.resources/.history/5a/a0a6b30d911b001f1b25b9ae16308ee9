//QUESTÃO 3

// pow(variável, expoente)

void iterativo(int numero){

	float numerador=0;
	float denominador=0;

	for (int i=1;i<numero+1;i++){
	numerador=pow(i,3);
	denominador=pow(10,i);
	printf("%d° -%d/%d\n",i,numerador,denominador);
	}
}

