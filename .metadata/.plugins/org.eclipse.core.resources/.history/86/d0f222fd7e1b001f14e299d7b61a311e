#include "biblioteca.h"

int main (void){
	setbuf(stdout,NULL);

	printf("Iterativa: %d\n\n", iterativa(5,7));
	printf("Recursiva Comum: %d\n\n", recursivaComum(5,7));
	int resultado recursivaDeCauda(5,1);

	return 0;
}

